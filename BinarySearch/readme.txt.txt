本例程为二分法查找例程

1.二分法查找的算法原理

1)首先，输入需要是一组已排好序的序列(默认从小到大)

2)初始化查找范围的左边界和右边界为0和序列的末尾

3)求出查找范围的中点处，比较中点处的值和待查找值的大小，如果小于中间值，则将右边界置为 (中间索引-1);
  
  反之，则将左边界置为  (中间索引+1)

4)反复3)直到左边界超出右边界，查找结束。

2.本例程支持的类对象范围
   
  必须支持比较大小
 
  必须支持标准输入输出

  否则会出bug。